## Running the app
1. Run `npm install`
2. Add database username and password to create-db.js
3. Run `npm run db:create-db`
4. Add database credentials to .env.local
5. Run `npm run dev`

## Running the tests
1. Run `npm run test`
